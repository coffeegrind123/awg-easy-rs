# Clippy configuration. The CI gate runs `cargo clippy --all-targets -- -D
# warnings`, so any new lint must be fixed (or explicitly allowed at the call
# site) before merge. No thresholds are relaxed here.
